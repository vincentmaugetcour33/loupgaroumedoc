{% if form is defined %}

   <form class="form-inline" method="post" action="{{ path('vm_blog_commentaire_list') }}" {#{% if commentaire is defined %}{{ path('vm_blog_commentaire_edit', {'id':commentaire.id}) }}{% else %}{{ path('vm_blog_commentaire_list') }}{% endif %}"#} {{ form_enctype(form) }}>
      <div class="form-group">{{ form_widget(form.auteur, { 'attr': {'class': 'form-control', 'placeholder': 'Auteur'} }) }}</div>
      <div class="form-group">{{ form_widget(form.contenu, { 'attr': {'class': 'form-control input-sm', 'placeholder':'Commentaire'} }) }}</div>
    <input type="submit" class="btn btn-default" />
    {#<input type="button" value="annuler"  onclick="document.location='/app_dev.php/commentaire/1'" class="btn btn-primary" />#}
  </form>
{% endif %}